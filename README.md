# 使い方  
expressをインストール  
npm init -y  
npm install express  
これでNode.jsサーバー起動  
node server.js  
publicと同じ階層にvideosフォルダを作成して  
そこに動画ファイルを保存する。  
もしくはフォルダの名前に合わせて設定を変える。  
動画ファイルの検索に特化しているので、  
できればmp4に限定したほうがいいかも。
